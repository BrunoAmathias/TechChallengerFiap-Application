apiVersion: v1
kind: Service
metadata:
  name: api-service

spec:
  type: ClusterIP

  selector:
    app: oficina-api

  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000